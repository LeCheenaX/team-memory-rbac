name: team_memory
version: 0.1.0
description: "Team Memory external provider for Hermes, backed by this repository's RBAC runtime."
# Temporarily disabled for manual recall tests:
#   - prefetch
#   - queue_prefetch
hooks:
  - sync_turn
  - on_pre_compress
  - on_session_end
  - on_memory_write
  - shutdown
